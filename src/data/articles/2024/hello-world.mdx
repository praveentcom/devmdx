---
title: "Hello World with MDX"
description: "First MDX post to validate the content pipeline"
date: "2024-01-15"
tags: ["nextjs", "react", "web-development", "javascript"]
published: true
image: "https://placehold.co/1200x628/10b981/ffffff.png?text=Hello+World+with+MDX"
---

# Hello, MDX!

This is a sample MDX post stored under a year folder.

## Why MDX

- Strong authoring experience
- Embed components
- Keep content in-repo

```ts
export function greet(name: string) {
  console.log(`Hello ${name}`);
}
```

## Why MDX

- Strong authoring experience
- Embed components
- Keep content in-repo

```ts
export function greet(name: string) {
  console.log(`Hello ${name}`);
}
```

## Why MDX

- Strong authoring experience
- Embed components
- Keep content in-repo

```ts
export function greet(name: string) {
  console.log(`Hello ${name}`);
}
```

## Why MDX

- Strong authoring experience
- Embed components
- Keep content in-repo

```ts
export function greet(name: string) {
  console.log(`Hello ${name}`);
}
```
